#### 唐文柱11.24日报

1,数据同步到doris
>![img_39.png](img_39.png)
>![img_40.png](img_40.png)
>![img_41.png](img_41.png)
// 更新该省份该日期的累计访问量
Long currentCount = dailyCountState.get(key);
if (currentCount == null) currentCount = 0L;
long newCount = currentCount + 1;
dailyCountState.put(key, newCount);

                        // 输出当前计数
                        JSONObject result = new JSONObject();
                        result.put("province", province);
                        result.put("log_date", date);
                        result.put("daily_visits", newCount);
                        result.put("update_time", new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(new Date()));

                        out.collect(result.toJSONString());